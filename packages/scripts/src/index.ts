import { formatNumToThousands, formatTime, isMobile } from './utils';
import type { LonersScripts } from './interface';

const lonersScripts: LonersScripts = {
  formatNumToThousands,
  formatTime,
  isMobile,
};

export default lonersScripts;
